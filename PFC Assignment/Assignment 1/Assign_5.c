#include <stdio.h>   // Header file for standard input/output functions

int main() {         // Main function where program execution begins
    printf("Hello, World!\n");  // Prints text on screen and moves to next line
    return 0;        // Returns 0 to the operating system (successful execution)
}